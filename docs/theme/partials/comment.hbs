{{#with comment}}
    {{#if hasVisibleComponent}}
        <div class="tsd-comment tsd-typography">
            {{#if shortText}}
                <div class="lead">
                    {{#markdown}}{{{shortText}}}{{/markdown}}
                </div>
            {{/if}}
            {{#if text}}
                {{#markdown}}{{{text}}}{{/markdown}}
            {{/if}}
            {{#if tags}}
                <dl class="tsd-comment-tags">
                    {{#each tags}}
                        {{#if isDeprecated}}
                            <dt class="tsd-is-deprecated">{{tagName}}</dt>
                            <dd>{{#markdown}}{{{text}}}{{/markdown}}</dd>
                        {{else}}
                            <dt>{{tagName}}</dt>
                            <dd>{{#markdown}}{{{text}}}{{/markdown}}</dd>
                        {{/if}}
                    {{/each}}
                </dl>
            {{/if}}
        </div>
    {{/if}}
{{/with}}